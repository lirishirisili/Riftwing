# התחלה מהירה עם Claude Code

1. חלץ את כל החבילה לתיקייה חדשה, למשל `riftwing`.
2. פתח את התיקייה ב-Claude Code.
3. ודא שכל תיקיות `docs`, `references`, `assets`, `manifests` ו-`prompts` נמצאות בשורש הפרויקט.
4. צרף לקלוד את תמונות ה-Reference במידת הצורך.
5. העתק אליו את כל התוכן של `CLAUDE_FIRST_MESSAGE.txt`.
6. אשר רק את משימת `prompts/00_bootstrap.md`.
7. לאחר שהפרויקט רץ ללא שגיאות, שלח: `Execute only prompts/01_player_movement.md and stop when complete.`
8. המשך לפי המספרים בלבד.

## חשוב

- שם המשחק הוא `RIFTWING`.
- הטקסט Starforge / Galaxy Rush שבתמונות הוא טקסט ישן בלבד.
- אין לבקש מקלוד לבנות את כל המשחק בבת אחת.
- יש להריץ ולבדוק כל שלב לפני שממשיכים.
- את התמונות המלאות משתמשים כהשוואה עיצובית, לא כרקע של המסך.
